version: '3'

services:
  db:
    image: postgres
    environment:
      POSTGRES_USER: example
      POSTGRES_PASSWORD: example2022
      POSTGRES_DB: test-microserviceone
      PGDATA: /data/postgres
    ports:
      - '5490:5432'